<h3>注文履歴一覧</h3>

  <th>購入日時</th>
  <th>購入者</th>
  <th>注文個数</th>
  <th>注文ステータス</th>
  
  
<% orders.each do |order| %>
      
  <%= link_to order.created_at.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(order) %>
  <%= order.customer.full_name %>
  <%= order.order_details.sum(:quantity) %><!-- 特定カラムの合計値 -->
  <%= order.status_i18n %><!-- 注文ステータスの日本語化 -->
  
<% end %>